<div>
            <div id="product-{{product._id}}">
                <div>
                    <p>{{product.code}}</p>
                    <h2>{{product.title}}</h2>
                    <p><span>Descripción:</span> {{product.description}}</p>
                    <p><span>Precio:</span> {{product.price}}</p>
                    <p><span>Categoría:</span> {{product.category}}</p>
                    <p><span>Stock:</span> {{product.stock}}</p>
                    <div>
                            <button onclick="addProductToCart('{{{product._id}}}')">Añadir al carrito</button>
                    </div>
                </div>  
            </div>
    </div>

<style>
    h1, h2, h3 {
        color: #333;
        margin-top: 0;
    }
    label {
        font-weight: bold;
        display: block;
        margin-bottom: 0.5rem;
    }
    input, select, textarea {
        width: 100%;
        padding: 0.5rem;
        margin-bottom: 1rem;
        border: 1px solid #ccc;
        border-radius: 4px;
        box-sizing: border-box;
    }
    button, .btn {
        background: #28a745;
        color: #fff;
        border: none;
        padding: 0.7rem 1.5rem;
        border-radius: 4px;
        cursor: pointer;
        font-size: 1rem;
        font-weight: bold;
        transition: background 0.2s;
    }
    button:hover, .btn:hover {
        background: #218838;
    }
    .card {
        background: #fafafa;
        border: 1px solid #e0e0e0;
        border-radius: 6px;
        padding: 1rem;
        margin-bottom: 1.5rem;
        box-shadow: 0 1px 4px rgba(0,0,0,0.04);
    }
    .flex-row {
        display: flex;
        gap: 1rem;
        align-items: center;
    }
    .center {
        text-align: center;
    }
</style>
